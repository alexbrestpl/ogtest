version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ogtest-backend
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend:/app
      - /app/node_modules
      - ./backend/statistics.db:/app/statistics.db
    environment:
      - NODE_ENV=production
    networks:
      - ogtest-network

networks:
  ogtest-network:
    driver: bridge
