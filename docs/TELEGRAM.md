# 🤖 Telegram бот

Интерактивный бот для получения статистики через Telegram.

## Настройка

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите `TELEGRAM_BOT_TOKEN`
3. Узнайте ваш `TELEGRAM_CHAT_ID` (например, через [@userinfobot](https://t.me/userinfobot))
4. Добавьте в `backend/.env`:
```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
```

## Команды

### /difficult
Получить топ-10 самых сложных вопросов

**Ответ:**
```
❗️ Топ-10 самых сложных вопросов:

1. Вопрос #142
   📊 Ошибок: 65.2% (показан 23 раз)

2. Вопрос #87
   📊 Ошибок: 58.1% (показан 31 раз)

...

💡 Минимум показов: 1 раз
🕐 Обновлено: 15.11.2025 19:15
```

## Безопасность

- Бот отвечает **только владельцу** (указанному в `TELEGRAM_CHAT_ID`)
- Попытки доступа от других пользователей логируются, но игнорируются
- Long polling с интервалом 2 секунды

## Архитектура

**Метод:** Long Polling (getUpdates API)
**Файл:** `backend/src/services/telegram.js`

**Процесс:**
1. Сервер каждые 2 сек опрашивает Telegram API
2. Получает новые сообщения
3. Проверяет авторизацию
4. Обрабатывает команды
5. Отправляет ответ

## Автоматические уведомления

При завершении каждой сессии бот автоматически отправляет:
- Режим теста (training/test)
- Результаты (правильные/неправильные)
- Процент успешности
- Время прохождения
- Вопросы с ошибками

## Расширение

Для добавления новых команд:
1. Добавьте обработчик в `handleCommand(message)`
2. Создайте функцию-обработчик (по аналогии с `handleDifficultCommand`)
3. Используйте `sendTelegramMessage(text)` для ответа
